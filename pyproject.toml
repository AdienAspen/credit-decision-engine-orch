[project]
name = "agentic-credit-risk-platform-block-a"
version = "0.1.0"
description = "Recovered Block A mock demonstrator for the agentic credit risk platform."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
